# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local project memory (never commit)
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local
.env
.env.*
backend/.env
backend/.env.*

# NotebookLM analytical snapshots (local only)
NotebookLM_Export/
CYBER_BRAIN.md

# Playwright MCP session artifacts (console logs, page snapshots)
.playwright-mcp/

# Isolated Codex feature worktrees
.worktrees/

# Client-delivered raw content artifacts (kept local; imported → committed JSON seed)
backend/data/
# Client gift PDFs for welcome email (not committed)
backend/assets/gifts/
